@model SERVERAPI.ViewModels.FertilizerDeleteViewModel

@{
    Layout = null;
}
<div id="target">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel">Fertilizer Details - @Model.act</h4>
    </div>

    <form asp-action="FertilizerDelete" asp-controller="Nutrients" data-ajax="true" data-ajax-method="POST" data-ajax-mode="REPLACE-WITH" data-ajax-update="#target">
        <div class="modal-body">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="fertilizerName"></label>
                <div>
                    @Model.fertilizerName<br />
                    <span asp-validation-for="fertilizerName"></span>
                </div>
            </div>
            <br />
        </div>
        <div class="modal-footer">
            <button class="btn" type="button" data-dismiss="modal">Cancel</button>
            <input class="btn btn-primary" type="submit" value="Delete" />
        </div>
        @Html.HiddenFor(x => x.fldName)
        @Html.HiddenFor(x => x.act)
        @Html.HiddenFor(x => x.id)
    </form>
</div>
