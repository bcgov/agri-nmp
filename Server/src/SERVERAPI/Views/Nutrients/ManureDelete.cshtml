@model SERVERAPI.ViewModels.ManureDeleteViewModel

@{
    Layout = null;
}
<div id="target">
    <div class="modal-header" style="background-color:red">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" style="background-color:red" id="myModalLabel">Manure Details - @Model.act</h4>
    </div>

    <form asp-action="ManureDelete" asp-controller="Nutrients" data-ajax="true" data-ajax-method="POST" data-ajax-mode="REPLACE-WITH" data-ajax-update="#target">
        <div class="modal-body">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="matType"></label>
                <div>
                    @Model.matType<br />
                    <span asp-validation-for="matType"></span>
                </div>
            </div>
            <br />
        </div>
        <div class="modal-footer">
            <button class="btn" type="button" data-dismiss="modal">Cancel</button>
            <input class="btn btn-danger" type="submit" value="Delete" />
        </div>
        @Html.HiddenFor(x => x.fldName)
        @Html.HiddenFor(x => x.act)
        @Html.HiddenFor(x => x.id)
    </form>
</div>
